"""Black-box acceptance tests — one per functional requirement.

These tests talk to the RUNNING system via API_BASE_URL.
NO app imports allowed — this is the fixed contract the verifier executes.
"""
